export type TModalType = 'order' | 'supplier' | 'product' | 'reset' | 'category';
export enum EFormModalType {
	PRODUCT = 'PRODUCT',
	SUPPLIER = 'SUPPLIER',
	ORDER = 'ORDER',
	CATEGORY = 'CATEOGRY',
	RESET = 'RESET',
}
